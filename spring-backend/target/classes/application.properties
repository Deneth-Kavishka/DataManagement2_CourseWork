# Server Configuration
server.port=5000
server.servlet.context-path=/api

# Oracle Database Configuration
spring.datasource.url=${ORACLE_DB_URL}
spring.datasource.username=${ORACLE_DB_USERNAME}
spring.datasource.password=${ORACLE_DB_PASSWORD}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.Oracle12cDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=urbanfood

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.urbanfood=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Jackson JSON Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=UTC

# File Upload Configuration
# Server Configuration
server.port=5000

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://0.0.0.0:27017/urbanfood

# Oracle Database Configuration
spring.datasource.url=jdbc:oracle:thin:@0.0.0.0:1521/XE
spring.datasource.username=urbanfood
spring.datasource.password=password
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=your-secret-key-here
jwt.expirationMs=86400000

# CORS Configuration
spring.web.cors.allowed-origins=http://0.0.0.0:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
spring.web.cors.allowed-headers=*

# Multipart Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Oracle Database Configuration
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XE
spring.datasource.username=urbanfood
spring.datasource.password=password
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/urbanfood

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# JWT Configuration
jwt.secret=your-secret-key
jwt.expiration=86400000
